<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.garima.campusmapper">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.SEND_RESPOND_VIA_MESSAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MySqliteDB" />
        <activity android:name=".Latlong1Activity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".HomeFragment" />
        <activity
            android:name=".Main_Page"
            android:label="@string/title_activity_main__page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PanicAlertActivity" />
        <activity android:name=".Splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC1s6tCiYTEuRmiDLlpcMem_19AK2hP5Yw" />

        <activity android:name=".graphmap" />
        <activity android:name=".Contact" />
        <activity android:name=".Coordinat" />
        <activity android:name=".PathActivity" />
        <activity
            android:name=".MapActivity"
            android:label="@string/title_activity_map" />
        <activity
            android:name=".Maps2Activity"
            android:label="@string/title_activity_maps2" />
        <activity
            android:name=".Maps1Activity"
            android:label="@string/title_activity_maps1" />
        <activity android:name=".GarimaActivity"></activity>
    </application>

</manifest>